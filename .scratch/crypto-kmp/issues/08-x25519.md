# 08 — X25519 (pure-K, RFC 7748)

Pure-Kotlin, constant-time X25519 key agreement over the field engine (07).

Status: ready-for-agent

Blocked by: 07

## What to build

- Pure-K `X25519(scalar, u)` — 255-bit scalar clamping + scalar multiplication over the field engine.
- No platform crypto in the common path.

## Acceptance

- [ ] RFC 7748 §5.2 X25519 test vectors (Alice/Bob/Charles) produce the published shared secrets.
- [ ] Wycheproof X25519 test vectors pass.
- [ ] `kover` shows 100% coverage on the pure-K path.
- [ ] `./gradlew detekt` (constant-time rule) is green.
- [ ] (Native path deferred to ticket 11/12 — this ticket is pure-K + vectors only.)

## Notes

- The native-fallback `actual` for X25519 is wired by ticket 11 (dispatch) + 12 (facade), not here.
